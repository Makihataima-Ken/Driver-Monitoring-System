# Driver Monitoring System — Python Dependencies
# Install with: pip install -r requirements.txt

# Core vision
opencv-python>=4.8.0
numpy>=1.24.0

# MediaPipe (FaceMesh)
mediapipe==0.10.14

# YOLO inference
# ultralytics is used as the *wrapper*; on the Pi the actual compute runs
# through NCNN, so you do NOT need PyTorch at runtime.
ultralytics>=8.2.0

# Pi 4 runtime backend (fast ARM NEON inference, ~40 MB instead of ~1 GB)
ncnn>=1.0.20240410

# Optional alternative backend:
# onnxruntime>=1.17.0

# NOTE: torch/torchvision are only needed on the machine where you EXPORT
# the model (tools/export_ncnn.py). Do not install them on the Pi.

# Configuration
pyyaml>=6.0

# Web API
fastapi>=0.109.0
uvicorn>=0.23.0

# System metrics
psutil>=5.9.0

# Optional: sound alerts
# pygame>=2.5.0

# Optional: Raspberry Pi camera
# picamera2>=0.3.12   # Pi only — install separately via apt on Pi

# Development / testing
# pytest>=7.0
# pytest-mock>=3.0
